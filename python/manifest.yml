---
applications:
- name: pythonapp
  #random-route: true
  #host: ${org}-${space}-pythonapp
  #domain: cfapps.us10.hana.ondemand.com
  route: pythonapp-${space}-${app-name}.${default-domain}
  path: ./
  memory: 256M
  disk-quota: 1024M
  buildpack: python_buildpack
  command: python server.py
  services:
  - vcplanner_mt-db
