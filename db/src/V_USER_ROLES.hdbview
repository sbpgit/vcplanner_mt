
VIEW "V_USER_ROLES" AS
SELECT DISTINCT U.USER,U.ROLE_NAME,RH.DESCRIPTION,R.READ,R.CREATE,R.UPDATE,R.DELETE FROM "CP_USER_ROLES" AS U
INNER JOIN "CP_ROLES" AS RH ON RH.ROLE_NAME = U.ROLE_NAME
INNER JOIN "CP_ROLE_DETAILS" AS R ON U.ROLE_NAME = R.ROLE_NAME