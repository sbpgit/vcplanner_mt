VIEW "V_ASMBCOMP_LOCM2" AS
SELECT 
SUM (A.COMP_QTY) AS COMP_QTY,
  WEEK_DATE,
  FACTORY_LOC,
  COMPONENT,
  VERSION,
  SCENARIO,
  MODEL_VERSION
  FROM "V_ASMREQ_PRODCONSDM2" AS A
 GROUP BY  WEEK_DATE,
 FACTORY_LOC,
 COMPONENT,
 VERSION,
 SCENARIO,
 MODEL_VERSION;