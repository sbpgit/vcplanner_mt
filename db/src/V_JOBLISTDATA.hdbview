VIEW "V_JOBLISTDATA"
AS
SELECT DISTINCT
	JOB_ID,
	JOB_NAME,
	JOB_STATUS,
	TOTAL_STEPS,
	TEMPLATE_ID,
	ACTIVE,
	SEQUENCE_ID,
	JOB_TYPE,
	LEVEL,
	JOB_STARTTIME,
    JOB_ENDTIME,
	CREATED_BY,
	VARIANT_FLAG,
	MAIN_SET,
	"SET"
FROM 
JS_JOB_TEMPLATEDETAILS
ORDER BY JOB_STARTTIME DESC;
