VIEW V_ASSEMBLY_COMP as
select distinct
	A.LOCATION_ID,
	A.ASSEMBLY,
	A.COMPONENT,
	CASE 
	WHEN B.PROD_DESC IS NULL
	THEN A.ASSEMBLY
	ELSE B.PROD_DESC
	END AS PROD_DESC
from CP_ASSEMBLY_COMP as A
LEFT OUTER JOIN CP_BOM_MAT as B
on 	B.MAT_CHILD= A.ASSEMBLY 
and B.LOCATION_ID = A.LOCATION_ID;