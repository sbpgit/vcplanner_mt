VIEW CP_V_UNIQUE_AGG AS
SELECT DISTINCT CAL_DATE, LOCATION_ID, PRODUCT_ID, SUM(UNIQUE_QTY) AS UNIQUE_AGG, "TYPE"
FROM CP_V_UNIQUE
GROUP BY CAL_DATE, LOCATION_ID, PRODUCT_ID, "TYPE"
ORDER BY CAL_DATE, LOCATION_ID, PRODUCT_ID, "TYPE";