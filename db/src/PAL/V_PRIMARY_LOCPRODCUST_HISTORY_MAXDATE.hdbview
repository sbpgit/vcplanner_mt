VIEW V_PRIMARY_LOCPRODCUST_HISTORY_MAXDATE AS
SELECT LOCATION_ID, PRODUCT_ID, CUSTOMER_GROUP, PRIMARY_ID, MAX(CAL_DATE) AS MAX_DATE
FROM V_PRIMARY_LOCPRODCUST_HISTORY
GROUP BY LOCATION_ID, PRODUCT_ID, CUSTOMER_GROUP, PRIMARY_ID;