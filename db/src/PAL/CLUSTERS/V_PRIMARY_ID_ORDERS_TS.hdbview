VIEW V_PRIMARY_ID_ORDERS_TS AS
SELECT DISTINCT MAT_AVAILDATE, LOCATION_ID, PRODUCT_ID, PRIMARY_ID, ORD_QTY FROM V_SALES_H 
--WHERE LOCATION_ID =  'PLUS' AND PRODUCT_ID = '000000000000000011' 
--AND DAYS_BETWEEN ("MAT_AVAILDATE", CURRENT_DATE)  < '728' AND DAYS_BETWEEN(CURRENT_DATE, MAT_AVAILDATE) < '182' 
--GROUP BY LOCATION_ID, PRODUCT_ID, PRIMARY_ID; 
--HAVING SUM(ORD_QTY) > '50'