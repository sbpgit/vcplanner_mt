VIEW V_LOCPROD_ORDERS_TS AS
SELECT MAT_AVAILDATE, LOCATION_ID, PRODUCT_ID, SUM(ORD_QTY) AS LOCPROD_ORDER_QTY FROM V_PRIMARY_ID_ORDERS_TS
GROUP BY MAT_AVAILDATE, LOCATION_ID, PRODUCT_ID
ORDER BY MAT_AVAILDATE, LOCATION_ID, PRODUCT_ID;