VIEW V_CLUSTER_PID_QTY AS
SELECT LOCATION_ID, PRODUCT_ID, PRIMARY_ID, PROFILE,CLUSTER_ID, SUM(ORD_QTY) AS AGG_ORD_QTY FROM V_CLUSTER_PID_QTY_TS
GROUP BY CLUSTER_ID, PROFILE, LOCATION_ID, PRODUCT_ID, PRIMARY_ID;