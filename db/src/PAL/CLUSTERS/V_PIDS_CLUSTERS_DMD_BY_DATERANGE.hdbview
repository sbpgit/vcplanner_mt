VIEW V_PIDS_CLUSTERS_DMD_BY_DATERANGE AS
SELECT LOCATION_ID, PRODUCT_ID, CLUSTER_ID, MODEL_PROFILE, PID, X, Y, SUM(DEMAND) AS DEMAND FROM V_PIDS_CLUSTERED_DEMAND 
GROUP BY  LOCATION_ID, PRODUCT_ID, CLUSTER_ID, MODEL_PROFILE, PID, X, Y
ORDER BY LOCATION_ID, PRODUCT_ID, CLUSTER_ID, MODEL_PROFILE, PID