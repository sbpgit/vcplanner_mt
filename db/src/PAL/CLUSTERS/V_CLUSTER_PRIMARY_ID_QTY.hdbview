VIEW V_CLUSTER_REPID_QTY AS
SELECT DISTINCT LOCATION_ID, PRODUCT_ID, PROFILE, CLUSTER_ID, 
MAX(QUANTITY) AS REP_PID_QTY FROM CP_CLUSTER_PRIMARY_ID_QTYS 
GROUP BY LOCATION_ID, PRODUCT_ID, PROFILE, CLUSTER_ID
ORDER BY CLUSTER_ID;