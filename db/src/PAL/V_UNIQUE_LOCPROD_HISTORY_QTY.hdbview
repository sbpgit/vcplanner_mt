VIEW V_UNIQUE_LOCPROD_HISTORY_QTY AS
SELECT LOCATION_ID, PRODUCT_ID, PRIMARY_ID, UNIQUE_ID, SUM(UNIQUE_QTY) AS UNIQUE_QTY
FROM V_UNIQUE_LOCPROD_HISTORY 
GROUP BY LOCATION_ID, PRODUCT_ID, PRIMARY_ID, UNIQUE_ID
ORDER BY LOCATION_ID, PRODUCT_ID, PRIMARY_ID, UNIQUE_ID;