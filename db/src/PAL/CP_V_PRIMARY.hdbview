VIEW CP_V_PRIMARY AS
SELECT DISTINCT CAL_DATE, LOCATION_ID, PRODUCT_ID, PRIMARY_ID, PRIMARY_QTY, "TYPE" FROM CP_V_PRIMARY_HISTORY
UNION SELECT CAL_DATE, LOCATION_ID, PRODUCT_ID, PRIMARY_ID, PRIMARY_QTY, "TYPE" FROM CP_V_PRIMARY_FUTURE;