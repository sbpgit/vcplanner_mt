VIEW CP_V_UNIQUE_OPTIONS_AGG AS
SELECT LOCATION_ID, PRODUCT_ID,CLASS_NUM, CHAR_NUM,
WEEK_DATE, SUM(OPT_QTY) AS OPT_SUM
FROM CP_V_UNIQUE_OPTIONS
GROUP BY LOCATION_ID, PRODUCT_ID, 
CLASS_NUM, CHAR_NUM, WEEK_DATE
ORDER BY  WEEK_DATE,LOCATION_ID, PRODUCT_ID, 
CLASS_NUM, CHAR_NUM;