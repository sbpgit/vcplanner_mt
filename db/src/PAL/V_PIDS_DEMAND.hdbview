VIEW V_PIDS_DEMAND AS 
SELECT LOCATION_ID, PRODUCT_ID, PRIMARY_ID, MAT_AVAILDATE AS START_DATE, MAT_AVAILDATE  AS END_DATE, SUM(ORD_QTY) AS DEMAND
FROM V_SALES_H 
GROUP BY  LOCATION_ID, PRODUCT_ID, PRIMARY_ID, MAT_AVAILDATE
ORDER BY START_DATE, END_DATE;