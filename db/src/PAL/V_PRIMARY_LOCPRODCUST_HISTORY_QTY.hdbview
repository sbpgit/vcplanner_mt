VIEW V_PRIMARY_LOCPRODCUST_HISTORY_QTY AS
SELECT LOCATION_ID, PRODUCT_ID, CUSTOMER_GROUP, PRIMARY_ID, SUM(UNIQUE_QTY) AS PRIMARY_QTY 
FROM V_UNIQUE_LOCPRODCUST_HISTORY_QTY 
GROUP BY LOCATION_ID, PRODUCT_ID, CUSTOMER_GROUP, PRIMARY_ID;