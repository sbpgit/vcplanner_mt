VIEW V_PRIMARY_LOCPROD_HISTORY_MAXDATE AS
SELECT LOCATION_ID, PRODUCT_ID, PRIMARY_ID, MAX(CAL_DATE) AS MAX_DATE
FROM V_PRIMARY_LOCPROD_HISTORY
GROUP BY LOCATION_ID, PRODUCT_ID, PRIMARY_ID, PRIMARY_ID;