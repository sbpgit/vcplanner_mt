VIEW "V_ORD_COUNT_DAILY" AS
SELECT
"LOCATION_ID",
"PRODUCT_ID",
MAT_AVAILDATE as "WEEK_DATE",
TO_VARCHAR (TO_DATE("MAT_AVAILDATE"), 'YYYYMMDD') as "WEEK_NO",
SUM("ORD_QTY") AS ORD_QTY
FROM V_SALES_H
GROUP BY
"LOCATION_ID",
"PRODUCT_ID",
"MAT_AVAILDATE";