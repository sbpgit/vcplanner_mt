VIEW "V_ACCESSNODES" AS
SELECT DISTINCT
A.LOCATION_ID,
B.LOCATION_DESC,
A.PRODUCT_ID,
B.PROD_DESC,
A.ACCESS_NODE,
A.CREATEDAT,
A.CREATEDBY,
A.MODIFIEDAT,
A.MODIFIEDBY
FROM CP_PROD_ACCNODE AS A
LEFT OUTER JOIN V_LOCPROD AS B
ON
A.LOCATION_ID = B.LOCATION_ID AND
A.PRODUCT_ID = B.PRODUCT_ID;