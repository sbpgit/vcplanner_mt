VIEW "V_PRODPRIMARYID" AS
    SELECT DISTINCT
        LOCATION_ID,
        PRODUCT_ID,
        PRIMARY_ID,
        MAT_AVAILDATE,
        SUM(ORD_QTY) AS QUANTITY
    FROM V_SALES_H
GROUP BY LOCATION_ID,
         PRODUCT_ID,
         MAT_AVAILDATE,
         PRIMARY_ID
ORDER BY QUANTITY DESC;