VIEW "V_BOMASSEMBLIES"
AS SELECT distinct 
	A."LOCATION_ID",
	B."LOCATION_DESC",
	A."PRODUCT_ID",
	B."PROD_DESC",
    B."PROD_TYPE"
FROM "CP_BOMHEADER" AS A
INNER JOIN "V_LOCPROD" AS B
ON A.LOCATION_ID = B.LOCATION_ID
 AND A.PRODUCT_ID = B.PRODUCT_ID;