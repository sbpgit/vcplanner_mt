VIEW "V_RANKED_CHAR_VALUES" AS
SELECT
    CHAR_NUM,
    CHAR_VALUE,
    ROW_NUMBER() OVER (
        PARTITION BY CHAR_NUM
        ORDER BY CHAR_VALUE ASC
    ) AS Rank_Column
FROM
    CP_CHAR_VALUES_MASTER;
 