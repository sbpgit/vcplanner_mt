VIEW "V_RTR_SNAPDATE" AS
SELECT 
	A."SNAP_TIMESTAMP",
	A."SNAPSHOT_DESC",
	A."FROM_DATE",
	A."TO_DATE",
    A."TYPE",
    B."FACTORY_LOC",
    B."LOCATION_ID",
    B."PRODUCT_ID",
    B."ITEM_NUM",
    B."COMPONENT",
    B."WEEK_DATE",
    B."REF_PRODID",
	MAX(B."SNAP_TIMESTAMP") AS "SNAP_TIMESTAMP_DATA"
FROM 
	"V_SNAPHEAD" AS A
	LEFT OUTER JOIN
	"V_RTR_SNAPDATA" AS B
	ON B."SNAP_TIMESTAMP" <= A."SNAP_TIMESTAMP"
WHERE A."TYPE" = 'RTR'
GROUP BY 
	A."SNAP_TIMESTAMP",
	A."SNAPSHOT_DESC",
	A."FROM_DATE",
	A."TO_DATE",
    A."TYPE",
    B."FACTORY_LOC",
    B."LOCATION_ID",
    B."PRODUCT_ID",
    B."ITEM_NUM",
    B."COMPONENT",
    B."WEEK_DATE",
    B."REF_PRODID"