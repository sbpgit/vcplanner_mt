VIEW "V_FUTUREDEMAND_VERSION" AS
SELECT DISTINCT IBP.LOCATION_ID,
 IBP.PRODUCT_ID,
 IBP.VERSION,
 IBP.SCENARIO,
 V.VERSION_NAME,
 V.SCENARIO_NAME
 FROM 
"CP_IBP_FUTUREDEMAND" AS IBP
INNER JOIN "CP_IBPVERSIONSCENARIO" AS V ON IBP.VERSION = V.VERSION AND IBP.SCENARIO = V.SCENARIO