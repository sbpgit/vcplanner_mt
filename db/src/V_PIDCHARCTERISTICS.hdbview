VIEW "V_PIDCHARCTERISTICS" AS
select distinct
		A.UNIQUE_ID,
		A.CHAR_NUM,
		B.CHAR_NAME,
		A.CHAR_VALUE,
		B.CHARVAL_DESC
	FROM CP_UNIQUE_ID_ITEM as A
	INNER JOIN V_CLASSCHARVAL as B
		ON A.CHAR_NUM = B.CHAR_NUM
       AND A.CHAR_VALUE = B.CHAR_VALUE