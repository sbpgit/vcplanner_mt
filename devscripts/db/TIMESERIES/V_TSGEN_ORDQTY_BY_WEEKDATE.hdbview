VIEW V_TSGEN_ORDQTY_BY_WEEKDATE AS
SELECT DISTINCT CAL_DATE, PRODUCT_ID, CLASS_NUM, 
SUM(ORD_QTY) AS ORDQTY
FROM V_TSGEN_ORDQTY_BYDATE 
--WHERE PRODUCT_ID = '8150RW'
GROUP BY CAL_DATE, PRODUCT_ID, CLASS_NUM
ORDER BY CAL_DATE, PRODUCT_ID, CLASS_NUM;